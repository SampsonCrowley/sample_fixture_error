<%= form_with(model: unique, local: true) do |form| %>
  <% if unique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unique.errors.count, "error") %> prohibited this unique from being saved:</h2>

      <ul>
        <% unique.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
